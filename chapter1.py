 #! Set up Env

 # Virtual Environment 
    # python3 -m venv odoo-12.0                 (create)
    # source ~/odoo-12.0/bin/activate           (activate)
    # set up .bashrc => alias

# init db 
    # ./odoo-bin -i base --save --config odoo.conf --stop-after-init -d odoo-14-test

# install requirement.txt 
    # pip -r requirements.txt
    
 #! Developer Tool (P38)
  #? debug tool, c
  #? debug terminal 